name: stats
description: Basic statistics extension functions.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  intl: ^0.18.1

dev_dependencies:
  flutter_lints: ^1.0.4
  test: ^1.17.0
