name: kmeans_cluster
description: A easy to use, thin wrapper around the kmeans package
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

dev_dependencies:
  test: ^1.24.3
