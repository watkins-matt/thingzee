name: repository_appw
description: Appwrite backend repository code.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  appwrite: ^9.0.0
  collection: ^1.17.1
  path: ^1.8.3
  path_provider: ^2.0.15
  repository:
    path: ../repository

dev_dependencies:
