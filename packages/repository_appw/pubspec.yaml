name: repository_appw
description: Appwrite backend repository code.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  appwrite: ^9.0.0
  collection: ^1.17.1
  log:
    path: ../log
  path: ^1.8.3
  path_provider: ^2.0.15
  repository:
    path: ../repository
  shared_preferences: ^2.2.0
  uuid: ^3.0.7

dev_dependencies:
  build_runner: ^2.4.4
  integration_test:
    sdk: flutter
  mockito: ^5.4.2
